import { Button } from "@nextui-org/button";

const LoginButton = () => {
    return (
        <>
            <Button color="primary" variant="shadow">
                Log in
            </Button>
            {/* Make call to server */}
        </>
    );
};

export default LoginButton;